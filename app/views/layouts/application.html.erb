<!DOCTYPE html>
<html lang="en">
   <head>
      <title>HealthApp</title>
      <%= csrf_meta_tags %>
      <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
      <meta name="viewport" content"width=device-width, initial-scale=1">
      </head class="container">
   <body id="page-top">
      <!-- Navigation -->
      <nav class="navbar navbar-expand-lg navbar-dark fixed-top" id="mainNav">
         <div class="container">
            <a class="navbar-brand js-scroll-trigger" href="#page-top">Smart Health Care-Patient Management System</a>
            <div class="collapse navbar-collapse" id="navbarResponsive">
               <ul class="navbar-nav text-uppercase ml-auto">
                  <div class="container">
                     <% if signed_in? %>
                     <p style="color: white;">Signed in as: <%= current_user.email %></p>
                     <%= button_to 'Sign Out', sign_out_path, method: :delete, class:"btn btn-primary" %>
                     <% else %>
                     <%= link_to 'Log In', sign_in_path, class:"btn btn-primary" %>
                     <% end %>
                  </div>
                  <div id="flash">
                     <% flash.each do |key, value| %>
                     <div class="flash <%= key %>"><%= value %></div>
                     <% end %>
                  </div>
               </ul>
            </div>
         </div>
      </nav>
      <!-- Header -->
      <header class="masthead">
         <div class="container">
            <div class="intro-text">
               <div class="intro-lead-in">
                  Welcome to the Smart Health<br> Care-Patient Management System
               </div>
            </div>
         </div>
      </header>
      <%= yield %>
   </body>
   <footer>
      <center>
         <p>x14562027 ~ Mark Leonard ~ Cloud Application Developement</p>
      </center>
   </footer>
</html>
